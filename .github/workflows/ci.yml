name: jHDF CI

on:
    push:
        branches: [ master]
    pull_request:
        branches: [ master ]
    schedule:
        - cron: '0 0 * * 4'

jobs:
    build:
        runs-on: ${{ matrix.os }}
        strategy:
            fail-fast: false
            matrix:
                os: [ 'macos-latest', 'ubuntu-latest', 'windows-latest', ['self-hosted', 'ARM64'] ]
                java: [ '8', '11', '17', '21' ]

        steps:
            - uses: actions/checkout@v4

            - name: Set up JDK (x64)
              uses: actions/setup-java@v4
              if: runner.arch != 'ARM64'
              with:
                  java-version: ${{ matrix.java }}
                  architecture: 'x64'
                  distribution: 'temurin'
                  cache: 'gradle'

            - name: Set up JDK (arm64)
              uses: actions/setup-java@v4
              if: runner.arch == 'ARM64'
              with:
                  java-version: ${{ matrix.java }}
                  architecture: 'aarch64'
                  distribution: 'temurin'
                  cache: 'gradle'

            - name: Install HDF5
              if: |
                  runner.arch != 'ARM64' &&
                  runner.os == 'Linux'
              run: >
                wget -q https://support.hdfgroup.org/ftp/HDF5/releases/hdf5-1.14/hdf5-1.14.1/bin/unix/hdf5-1.14.1-2-Std-ubuntu2204_64.tar.gz
                  tar -xzf hdf5-1.14.1-2-Std-ubuntu2204_64.tar.gz
                  ./hdf/HDF5-1.14.1-Linux.sh --skip-license
                  ls -la
                  echo "$GITHUB_WORKSPACE/HDF_Group/HDF5/1.14.1/bin" >> $GITHUB_PATH
                  echo $GITHUB_PATH

            - name: h5dump version
              if: |
                  runner.arch != 'ARM64' &&
                  runner.os == 'Linux'
              run: h5dump --version

            - name: Gradle Build
              working-directory: jhdf
              run: ./gradlew build

            - name: Gradle Build Jars
              working-directory: jhdf
              run: ./gradlew jar sourcesJar javadocJar

            - name: Gradle Stop Daemon
              working-directory: jhdf
              run: ./gradlew --stop

            - name: Archive junit results
              if: always()
              uses: actions/upload-artifact@v4
              with:
                name: junit-report-${{ matrix.os }}-${{ matrix.java }}
                path: jhdf/build/reports/tests/test
                if-no-files-found: error
